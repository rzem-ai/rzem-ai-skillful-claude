# Personal memory

Global preferences that apply across every project on this machine.

- Prefer concise diffs; explain non-obvious choices inline.
- Default to TypeScript strict mode for new packages.
- Never commit secrets; use environment variables.
- Run the test suite before opening a PR.

## Style

- Match the surrounding code's conventions over personal preference.
- Keep functions small and pure where practical.
